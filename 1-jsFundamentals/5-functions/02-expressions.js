let hey = function hi() {
    //we have a variable named (hey) to represent the function hi
    console.log('hi');
}

/*
 -difference between declaration and expression:
    -declaration gets hoisted
    -expression does NOT get hoisted
*/

hey();